node_modules/
*.sqlite
antiguos/
# Andamios servidos por pm2 para validar cambios de cliente en el navegador
# (p. ej. _preview_detalle.html): útiles en el momento, nunca parte del juego.
public/_preview_*.html
# El Excel de Toto (y cualquier export suyo): son SUS ideas de cartas sin publicar.
# Vive en disco para consultarlo, pero NUNCA se versiona ni sale del servidor.
*.csv
docs/Cartas KG.csv
# El Excel original de Toto y sus ficheros de bloqueo: misma razon que el CSV.
docs/Cartas KG.xlsx
# El enlace privado de Dropbox del Excel: vive SOLO en este servidor.
docs/.cartas-kg.url
docs/~$*
docs/.Cartas KG.csv.anterior
# Apuntes tecnicos de Toto para estudiar (CI, tests, git...): notas personales, no documentacion
# del proyecto. Se quedan en el servidor a proposito.
docs/apuntes-tecnicos.md
# El primer README, redactado por Claude en su estilo: se conserva como material de consulta
# (tiene los mismos datos), pero el bueno es el que escribe Toto.
README_full_Claude.md
